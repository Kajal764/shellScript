#!/bin/bash -x

echo "enter a no"
read no
count=0
for (( i=2;i<n;i++ )) 
do
	if [[ `expr $no % $i` -eq 0 ]]
	then
		count=(( $count + 1 ))
		echo $count
	fi
done

if [ $count -gt 0 ]
	then
	echo "this is not prime no"
else
	echo "this no is prime no"
fi

